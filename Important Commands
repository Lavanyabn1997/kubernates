eksctl create cluster -f cluster.yaml                           # Create an EKS cluster using configuration from cluster.yaml

eksctl create cluster --name my-eks-cluster --nodegroup-name my-worker-node --node-type t2.micro --nodes 3 --managed --region us-west-2
# Create a managed EKS cluster named "my-eks-cluster" with 3 t2.micro worker nodes in us-west-2 region

eksctl delete cluster --name my-eks-cluster --region us-east-1  # Delete the EKS cluster named "my-eks-cluster" from us-east-1 region

kubectl cluster-info                                            # Display Kubernetes cluster master and service information

kubectl get nodes                                               # List all worker/master nodes in the cluster

kubectl describe node <node-name>                               # Show detailed information about a specific node

kubectl label nodes node1 gpu=true                              # Add label gpu=true to node named node1

kubectl get nodes -o wide                                       # List nodes with extra details like internal IP, OS, kernel version

kubectl drain <node-name> --ignore-daemonsets                   # Safely evict workloads from a node before maintenance

kubectl uncordon <node-name>                                    # Mark node as schedulable again after maintenance

kubectl cordon <node-name>                                      # Mark node as unschedulable to stop new pod scheduling

kubectl top nodes                                               # Show CPU and memory usage of nodes (Metrics Server required)

kubectl get nodes --show-labels                                 # List nodes along with all labels attached to them

kubectl taint nodes <node-name> key=value:NoSchedule            # Prevent pods from scheduling unless they tolerate this taint

kubectl taint nodes <node-name> key=value:NoSchedule-           # Remove the taint from the node

kubectl get pods                                                # List all pods in the current namespace

kubectl get pods -o wide                                        # List pods with node assignment and IP details

kubectl describe pod <pod-name>                                 # Show detailed information and events of a pod

kubectl delete pod <pod-name>                                   # Delete a specific pod

kubectl logs <pod-name>                                         # View logs generated by a pod container

kubectl exec -it <pod-name> -- /bin/bash                        # Open interactive bash shell inside the pod container

kubectl get pods -w                                             # Watch pod changes live in real time

kubectl get deployments                                         # List all deployments

kubectl describe deployment <name>                              # Show deployment details including replicas and strategy

kubectl apply -f deployment.yaml                                # Create or update deployment using YAML file

kubectl delete deployment <name>                                # Delete a deployment

kubectl rollout status deployment <name>                        # Check deployment rollout progress/status

kubectl rollout history deployment <name>                       # Show rollout revision history of deployment

kubectl rollout undo deployment <name>                          # Rollback deployment to previous revision

kubectl rollout undo deployment <name> --to-revision=1          # Rollback deployment to a specific revision number

k rollout status deployment <name>                              # Shortcut alias (k=kubectl) to check rollout status

kubectl rollout restart deployment <name>                       # Restart all pods in the deployment

kubectl rollout history deployment <name> --revision=5          # Show detailed information for revision 5

kubectl get deployment <name> -o yaml > backup.yaml             # Export deployment YAML configuration to backup.yaml

kubectl set image deployment <name> nginx=nginx:1.28            # Update nginx container image version in deployment

kubectl get svc                                                 # List all services

kubectl describe svc <name>                                     # Show detailed information about a service

kubectl apply -f svc.yaml                                       # Create/update service from YAML definition

kubectl delete svc <name>                                       # Delete a service

kubectl get all                                                 # Show all resources in current namespace

kubectl describe <resource> <name>                              # Show detailed information about any resource

kubectl logs -f <pod>                                           # Stream pod logs continuously in real time

kubectl get events                                              # Display cluster events sorted by time

kubectl api-resources                                           # List all supported Kubernetes resource types

kubectl explain pod                                             # Show documentation for Pod resource fields

kubectl create namespace <name>                                 # Create a new namespace

kubectl get ns                                                  # List all namespaces

kubectl apply -f pod.yaml -n <name>                             # Deploy pod into specified namespace

kubectl get pods -n <name>                                      # List pods inside a namespace

kubectl get all -n <name>                                       # List all resources inside a namespace

kubectl delete namespace <name>                                 # Delete a namespace and all resources inside it

kubectl get namespaces -l app=hiqode-demo                       # List namespaces matching label app=hiqode-demo

kubectl get rs                                                  # List ReplicaSets

kubectl describe rs <replicaset-name>                           # Show detailed information about ReplicaSet

kubectl apply -f replicaset.yaml                                # Create/update ReplicaSet using YAML file

kubectl delete rs <replicaset-name>                             # Delete a ReplicaSet

kubectl get rs -A                                               # List ReplicaSets across all namespaces

kubectl get rs -o wide                                          # List ReplicaSets with extended details

kubectl scale rs <name> --replicas=5                            # Scale ReplicaSet to 5 replicas

kubectl get ingress -n <namespaces> -w                          # Watch ingress resources continuously in a namespace

kubectl logs -n kube-system -l app.kubernetes.io/name=aws-load-balancer-controller --tail=50
# Show last 50 logs from AWS Load Balancer Controller pods

kubectl describe ingress hiqode-ingress -n hiqode               # Display detailed ingress configuration and events

kubectl get pods -n kube-system                                 # List system pods in kube-system namespace

helm uninstall aws-load-balancer-controller -n kube-system      # Remove AWS Load Balancer Controller Helm release
